# All Pythton class files

This directory contains the core Python class files  code the Movie Genre Classification project.

## Purpose

The modules in this folder are responsible for:

- Loading the dataset
- Data preprocessing
- Saving the trained models

## Planned Modules

- `data_loader.py` – Load the dataset
- `preprocessing.py` – Data cleaning and preprocessing
- `models.py` – Saving the trained models